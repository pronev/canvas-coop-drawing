{
  "name": "canvas-websocket-server",
  "version": "0.0.2",
  "description": "server",
  "scripts": {
    "dev": "nodemon --ignore tests/ --watch src -e ts --exec ts-node src/index.ts",
    "start": "node dist",
    "build": "tsc",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pronev",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.24",
    "@types/ws": "^8.5.10",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
